import 'package:flutter/material.dart';

class SecondPage2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('이용 서비스'),backgroundColor: Colors.white,),
      body: Center(child: Text('2')),

    );
  }
}
